# NPM Configuration
# Note: Some of the previous configs were pnpm-specific and have been removed

# Enable strict peer dependency resolution (npm 7+)
legacy-peer-deps=false

# Use exact versions for better reproducibility
save-exact=true

# Disable package-lock.json generation if using different package manager
# package-lock=false
