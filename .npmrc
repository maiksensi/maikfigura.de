# Pin every dependency to an exact version when running `pnpm add`.
# Range prefixes (^, ~) let transitive major/minor bumps slip into the build
# and are the reason a Renovate range once dragged in a broken major.
# Renovate still proposes updates as explicit, reviewable pin changes.
save-exact=true
save-prefix=
